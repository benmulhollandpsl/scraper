 {{#each articles}}

<div class="card-body">
                        <div class="row">
                            <div class="col-8">
                                <p>{{summary}}</p>
                            </div>
                            <div class="col-4 buttonColumn">
                                <button type="button" class="btn btn-primary saveArticleButton" data-id={{id}}>Save Article</button>
                            </div>
                        </div>
                    </div>